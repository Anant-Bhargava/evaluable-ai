from EvaluableAI import EvaluableAI


def main():
     # Replace with your actual JSON file path
    # Instantiate the EvaluateAI object with the provided JSON file path
    evaluator = EvaluableAI()
    
    # Here you can add more code to use the evaluator object
    # For example:
    # result = evaluator.evaluate()

# This is a standard Python practice to ensure that the main function
# is executed only when the script is run directly, not when imported as a module.
if __name__ == "__main__":
    main()